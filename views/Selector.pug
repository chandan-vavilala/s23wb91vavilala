extends layout

block content
  h1= title
  - var images = ["A108.jpg","A132.jpg","A543.jpg","A777.jpg","A555.jpg"]
  - for(let i=0; i<3;i++){
    -var num = images[Math.floor(Math.random() * 5)]
    img(src= "/images/"+num, alt= num, style={width: "300px",height: "300px"})
  -}